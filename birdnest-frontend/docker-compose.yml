version: '3.8'

services:
  app:
    image: filipeller/apps:birdnest-frontend
    container_name: birdnest-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
